
import Image from "next/image";

import { Metadata } from "next";
export const metadata = {
  title: "Perfil | Umov",
  description: "Esta pgina es sobre tu perfil",
  // other metadata
};

const Profile = () => {
  return (
    <>
    <h1>Profile</h1>
    </>
  )
}

export default Profile;